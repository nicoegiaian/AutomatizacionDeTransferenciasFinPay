<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <style>
        @page { margin: 0px; }
        body {
            font-family: Arial, sans-serif; font-size: 11px;
            margin-top: 130px; margin-left: 40px; margin-right: 40px; margin-bottom: 80px;
        }
        .header-img { position: fixed; top: 0; left: 0; width: 100%; height: auto; z-index: -1000; }
        .footer-img { position: fixed; bottom: 20px; right: 20px; width: 150px; height: auto; z-index: -1000; }
        table { width: 100%; border-collapse: collapse; margin-bottom: 15px; }
        th, td { padding: 4px; text-align: left; vertical-align: top; }
        .amount { text-align: right; white-space: nowrap; }
        .section-header { font-weight: bold; margin-top: 15px; border-bottom: 1px solid #000; text-transform: uppercase; }
        .total-row td { border-top: 1px solid #000; font-weight: bold; padding-top: 5px;}
        .neto-amount { font-size: 14px; font-weight: bold; text-align: right; }
        .footer-note { position: fixed; bottom: 30px; left: 40px; right: 40px; text-align: center; font-size: 9px; color: #666; font-style: italic; }
    </style>
</head>
<body>
    {% if images.encabezado %}
        <img src="data:image/png;base64,{{ images.encabezado }}" class="header-img">
    {% endif %}
    {% if images.pie %}
        <img src="data:image/png;base64,{{ images.pie }}" class="footer-img">
    {% endif %}

    <table>
        <tr><td style="width:25%; font-weight:bold;">Razón Social</td><td>{{ report.header.razon_social }}</td></tr>
        <tr><td style="font-weight:bold;">CUIT</td><td>{{ report.header.cuit }}</td></tr>
        <tr><td style="font-weight:bold;">Período</td><td>{{ report.header.periodo }}</td></tr>
        <tr><td style="font-weight:bold;">N° Resumen</td><td>{{ report.header.nro_resumen }}</td></tr>
    </table>

    <div class="section-header">DETALLE DE LAS TRANSACCIONES</div>
    <table>
        <thead><tr><th style="width:50%"></th><th class="amount">Cant.</th><th class="amount">MONTO BRUTO</th></tr></thead>
        <tbody>
            <tr><td>Ventas Débito</td><td class="amount">{{ report.items.debito.cant }}</td><td class="amount">$ {{ report.items.debito.monto|number_format(2,',','.') }}</td></tr>
            <tr><td>Ventas Crédito</td><td class="amount">{{ report.items.credito.cant }}</td><td class="amount">$ {{ report.items.credito.monto|number_format(2,',','.') }}</td></tr>
            <tr><td>Ventas QR</td><td class="amount">{{ report.items.qr.cant }}</td><td class="amount">$ {{ report.items.qr.monto|number_format(2,',','.') }}</td></tr>
            <tr class="total-row"><td>TOTAL</td><td class="amount">{{ report.totales.transacciones }}</td><td class="amount">$ {{ report.totales.bruto|number_format(2,',','.') }}</td></tr>
        </tbody>
    </table>

    <div class="section-header">DETALLE DESCUENTOS</div>
    <table>
        <tr><td>Costo de servicio</td><td class="amount">$ {{ report.totales.costo_servicio|number_format(2,',','.') }}</td></tr>
        <tr><td>Costo financiación</td><td class="amount">$ {{ report.totales.costo_financiacion|number_format(2,',','.') }}</td></tr>
        <tr><td>Aranceles tarjetas</td><td class="amount">$ {{ report.totales.aranceles|number_format(2,',','.') }}</td></tr>
        <tr><td>IVA</td><td class="amount">$ {{ report.totales.iva|number_format(2,',','.') }}</td></tr>
        <tr><td>Otros impuestos</td><td class="amount">$ {{ report.totales.otros_impuestos|number_format(2,',','.') }}</td></tr>
    </table>

    <table style="width:60%; margin-top:20px;">
        <tr><td style="font-weight:bold;">NETO PERCIBIDO</td><td class="neto-amount">$ {{ report.totales.neto_percibido|number_format(2,',','.') }}</td></tr>
    </table>
    
    <table style="width:60%; margin-top:10px;">
        <tr><td>EN CUENTA COMERCIO</td><td class="amount">$ {{ report.totales.en_cuenta_comercio|number_format(2,',','.') }}</td></tr>
        <tr><td>EN CC BATERIAS MOURA</td><td class="amount">$ {{ report.totales.en_cc_moura|number_format(2,',','.') }}</td></tr>
        <tr><td style="padding-top:10px;">BENEFICIO CREDMOURA</td><td class="amount" style="padding-top:10px;">$ {{ report.totales.beneficio_credmoura|number_format(2,',','.') }}</td></tr>
    </table>

    <div class="footer-note">DOCUMENTO NO VALIDO PARA EL COMPUTO DE EGRESOS Y/O IMPUESTOS DETALLADOS</div>
</body>
</html>